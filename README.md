# TSA Wait Time Proxy (Flask + Render)

A simple proxy API that fetches live TSA security checkpoint wait times from  
`apps.tsa.dhs.gov` and returns them with CORS enabled for use in digital signage platforms  
(Appspace, HTML dashboards, etc.).

## Live Endpoint (after deploy)

